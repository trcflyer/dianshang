<!--index.wxml-->
<view class="containe">
<swiper indicator-dots="true"
  autoplay="true" interval="2000" duration="1000">
  <block wx:for="{{imgUrls}}" wx:key="topswiper">
    <swiper-item>
        <image style="width: {{winWidth}}px;" mode="scaleToFill" src="{{host}}{{item.cover}}"></image>
    </swiper-item>
  </block>
</swiper>
<view class="swiper-tab" >  
    <view class="swiper-tab-list {{currentTab==0 ? 'on' : ''}}" data-current="0" bindtap="swichNav">{{categoryList[0].name}}</view>  
    <view class="swiper-tab-list {{currentTab==1 ? 'on' : ''}}" data-current="1" bindtap="swichNav">{{categoryList[1].name}}</view>  
    <view class="swiper-tab-list {{currentTab==2 ? 'on' : ''}}" data-current="2" bindtap="swichNav">{{categoryList[2].name}}</view>  
    <view class="swiper-tab-list {{currentTab==3 ? 'on' : ''}}" data-current="3" bindtap="swichNav">{{categoryList[3].name}}</view>
    <view class="swiper-tab-list {{currentTab==4 ? 'on' : ''}}" data-current="4" bindtap="swichNav">{{categoryList[4].name}}</view>
</view>  
<view class="search">
 <input class="input" confirm-type="search" name="userName" placeholder="请输入商品名" bindconfirm="searchInput"/>
 <image  src="../../image/search.png"></image>
 </view>
<swiper current="{{currentTab}}" class="swiper-box" duration="300" style="height:{{winHeight - 31}}px" bindchange="bindChange">  
    <!-- 1 -->  
    <swiper-item > 
      <block wx:for="{{products}}" class="flex-wrp"  wx:for-index="idx" wx:for-item="itemName" wx:key="pswiper1">
        <view wx:if="{{idx % 2==0}}" class="product-item-left" data-product-id="{{itemName.id}}" bindtap="prodectTap" >
          <image class="product-image" mode="scaleToFill" src="{{host}}{{itemName.cover}}"></image>
          <text class="product-desc">{{itemName.name}}</text>
          <text class="product-price">￥:{{itemName.price}}</text>
        </view>
        <view wx:if="{{idx % 2==1}}" class="product-item-right" data-product-id="{{itemName.id}}" bindtap="prodectTap">
          <image class="product-image" mode="scaleToFill" src="{{host}}{{itemName.cover}}"></image>
          <text class="product-desc">{{itemName.name}}</text>
          <text class="product-price">￥:{{itemName.price}}</text>
        </view>
      </block>
    </swiper-item>
    <!-- 2 -->
    <swiper-item>     
       <block wx:for="{{products}}" class="flex-wrp"  wx:for-index="idx" wx:for-item="itemName" wx:key="pswiper2">
        <view wx:if="{{idx % 2==0}}" class="product-item-left" data-product-id="{{itemName.id}}" bindtap="prodectTap" >
          <image class="product-image" mode="scaleToFill" src="{{host}}{{itemName.cover}}"></image>
          <text class="product-desc">{{itemName.name}}</text>
          <text class="product-price">￥:{{itemName.price}}</text>
        </view>
        <view wx:if="{{idx % 2==1}}" class="product-item-right" data-product-id="{{itemName.id}}" bindtap="prodectTap">
          <image class="product-image" mode="scaleToFill" src="{{host}}{{itemName.cover}}"></image>
          <text class="product-desc">{{itemName.name}}</text>
          <text class="product-price">￥:{{itemName.price}}</text>
        </view>
      </block> 
    </swiper-item> 
    <!-- 3 -->
    <swiper-item>  
       <block wx:for="{{products}}" class="flex-wrp"  wx:for-index="idx" wx:for-item="itemName" wx:key="pswiper3">
        <view wx:if="{{idx % 2==0}}" class="product-item-left" data-product-id="{{itemName.id}}" bindtap="prodectTap" >
          <image class="product-image" mode="scaleToFill" src="{{host}}{{itemName.cover}}"></image>
          <text class="product-desc">{{itemName.name}}</text>
          <text class="product-price">￥:{{itemName.price}}</text>
        </view>
        <view wx:if="{{idx % 2==1}}" class="product-item-right" data-product-id="{{itemName.id}}" bindtap="prodectTap">
          <image class="product-image" mode="scaleToFill" src="{{host}}{{itemName.cover}}"></image>
          <text class="product-desc">{{itemName.name}}</text>
          <text class="product-price">￥:{{itemName.price}}</text>
        </view>
      </block>  
    </swiper-item> 
    <!-- 4 -->
    <swiper-item>   
       <block wx:for="{{products}}" class="flex-wrp"  wx:for-index="idx" wx:for-item="itemName" wx:key="pswiper4">
        <view wx:if="{{idx % 2==0}}" class="product-item-left" data-product-id="{{itemName.id}}" bindtap="prodectTap" >
          <image class="product-image" mode="scaleToFill" src="{{host}}{{itemName.cover}}"></image>
          <text class="product-desc">{{itemName.name}}</text>
          <text class="product-price">￥:{{itemName.price}}</text>
        </view>
        <view wx:if="{{idx % 2==1}}" class="product-item-right" data-product-id="{{itemName.id}}" bindtap="prodectTap">
          <image class="product-image" mode="scaleToFill" src="{{host}}{{itemName.cover}}"></image>
          <text class="product-desc">{{itemName.name}}</text>
          <text class="product-price">￥:{{itemName.price}}</text>
        </view>
      </block>  
    </swiper-item>
    <!-- 5 -->
    <swiper-item>  
       <block wx:for="{{products}}" class="flex-wrp"  wx:for-index="idx" wx:for-item="itemName" wx:key="pswiper5">
        <view wx:if="{{idx % 2==0}}" class="product-item-left" data-product-id="{{itemName.id}}" bindtap="prodectTap" >
          <image class="product-image" mode="scaleToFill" src="{{host}}{{itemName.cover}}"></image>
          <text class="product-desc">{{itemName.name}}</text>
          <text class="product-price">￥:{{itemName.price}}</text>
        </view>
        <view wx:if="{{idx % 2==1}}" class="product-item-right" data-product-id="{{itemName.id}}" bindtap="prodectTap">
          <image class="product-image" mode="scaleToFill" src="{{host}}{{itemName.cover}}"></image>
          <text class="product-desc">{{itemName.name}}</text>
          <text class="product-price">￥:{{itemName.price}}</text>
        </view>
      </block>
    </swiper-item>   
</swiper>
</view>